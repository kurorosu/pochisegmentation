"""セグメンテーション訓練設定テンプレート.

このファイルをコピーして, プロジェクトに合わせて編集してください.
"""

# =============================================================================
# モデル設定
# =============================================================================
# アーキテクチャ: "Unet" or "DeepLabV3Plus"
architecture = "Unet"

# エンコーダー: resnet18, resnet34, resnet50, efficientnet-b0, etc.
encoder_name = "resnet34"

# ImageNet事前学習重みを使用
pretrained = True

# クラス数 (背景含む)
num_classes = 2

# =============================================================================
# データ設定
# =============================================================================
# VOC形式データセットのルートディレクトリ
data_root = "data/VOC2012"

# 分割 (train, val, trainval)
train_split = "train"
val_split = "val"

# 画像サイズ
image_size = 256

# バッチサイズ
batch_size = 16

# DataLoaderのワーカー数
num_workers = 4

# =============================================================================
# 訓練設定
# =============================================================================
# エポック数
epochs = 10

# 学習率
learning_rate = 1e-3

# オプティマイザ: "Adam", "AdamW", "SGD"
optimizer = "AdamW"

# =============================================================================
# 損失関数
# =============================================================================
# 損失関数: "DiceLoss", "FocalLoss", "JaccardLoss"
loss = "DiceLoss"

# 損失関数のパラメータ
loss_params = {"mode": "multiclass"}

# =============================================================================
# スケジューラ
# =============================================================================
# スケジューラ: "CosineAnnealingLR", "StepLR", "ReduceLROnPlateau", None
scheduler = "CosineAnnealingLR"

# スケジューラのパラメータ
scheduler_params = {"T_max": 100}

# =============================================================================
# 層別学習率
# =============================================================================
# 層別学習率を有効化
enable_layer_wise_lr = True

# エンコーダー学習率 (事前学習済みなので小さめ)
encoder_lr = 1e-4

# デコーダー学習率
decoder_lr = 1e-3

# =============================================================================
# Early Stopping
# =============================================================================
# Early Stopping の patience (None または 0 で無効)
# 指定したエポック数の間, 検証指標 (mIoU) が改善しなければ訓練を停止
early_stopping_patience = None

# =============================================================================
# AMP (Automatic Mixed Precision)
# =============================================================================
# 混合精度訓練を有効化 (メモリ削減・高速化, CUDA専用)
enable_amp = False

# =============================================================================
# ワークスペース設定
# =============================================================================
# ワークスペースのベースディレクトリ
work_dir = "work_dirs"

# 使用デバイス: "cuda" or "cpu"
device = "cuda"

# =============================================================================
# 可視化・ログ設定
# =============================================================================
# メトリクスエクスポートを有効化
enable_metrics_export = True
